	#Prima parte
	def f_sum(a, b):
		sum = a + b
		return sum
	a = 1
	b = 2
	sum = 3
	f_sum(100, 100)
	print(sum,"\n")

	#Seconda parte
	def f_sum(a, b):
		sum = a + b
		return sum
	a = 1
	b = 2
	sum = 3
	print(f_sum(100, 100))

	#Nella prima parte del codice a schermo viene stampata la variabile sum che nel nostro caso è =3. Nella seconda parte, invece, viene stampato il valore calcolato dalla funzione f_sum, nel nostro caso =200.
